#include <iostream>
#include<cmath>
using std::cout;
using std::cin;
using namespace std;

int main()
{
    bool found =true;
    // search algorithm -> if found, found = true;

    cout << std::boolalpha << found << std::endl;
    if(found) cout << "This is true";
}
